# MSME Synthetic Persona Verification

Run the verification script (generates cohort if missing):

```bash
python notebooks/verify_personas.py
```

Or open this folder and run `verify_personas.py` after installing dependencies.

Outputs:
- `notebooks/output/persona_summary.csv` — per-persona mean metrics
- `notebooks/output/persona_comparison.png` — bar charts comparing personas
